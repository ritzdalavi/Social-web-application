import React from 'react';
import './Story.css';

const Story = ({ username, image, hasUnseenStory }) => {
  // Story component implementation
  return (
    <div className="story">
      {/* Story content */}
    </div>
  );
};

export default Story; // Make sure to use default export