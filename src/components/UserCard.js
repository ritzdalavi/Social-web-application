import React from 'react';
import './UserCard.css';

const UserCard = ({ username, name, image, relation, showFollowButton, isCurrentUser }) => {
  // UserCard component implementation
  return (
    <div className="user-card">
      {/* User card content */}
    </div>
  );
};

export default UserCard; // Make sure to use default export